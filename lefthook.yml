pre-commit:
  parallel: true
  jobs:
    - name: lint
      glob: "*.go"
      run: golangci-lint run -c ./.golangci.yml {staged_files}
    - name: swagger docs update
      run: make swagger && git add docs/*