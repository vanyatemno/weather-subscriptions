pre-commit:
  parallel: true
  jobs:
    - name: lint
      glob: "./{internal,cmd,api}/**/*.go"
      run: make lint-staged FILES="{staged_files}"
    - name: swagger docs update
      run: make swagger && git add docs/*